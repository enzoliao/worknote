安装完成Pyton3之后，需要修改环境变量，用export PATH = $PATH:/<python_dir_place>/bin 把该目录加入到linux环境中，这样输入python3 就可以运行新版本的python了。
如果要每次启动shell时都可以运行python3，需要修改/etc/bash.bashrc 这一文件，将上一句添加到其中，注意这一文件是只读文件，需要用chmod a+w修改它的属性。
